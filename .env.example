# This static site does not load .env files or VITE_* variables.
# Configure these two properties in Google Apps Script > Project Settings > Script Properties.
# Never put an actual token in this file or a front-end build variable.
SPREADSHEET_ID=
API_TOKEN=
