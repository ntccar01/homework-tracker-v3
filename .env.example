# ============================================
# 環境變數範本 — 填入你的金鑰後另存為 .env
# ⚠️ .env 已在 .gitignore 中排除，不會被 commit
# ============================================

# Google Apps Script 部署網址
VITE_GAS_URL=

# API Token（需與 GAS 端一致）
VITE_API_TOKEN=
