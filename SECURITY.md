# 安全與資料保護

本專案的原始碼可公開。實際 API Token、GAS 部署設定、試算表與學生資料須由使用者私下保存。

## 已有保護

- 所有資料 API 先驗證 Token，再讀取試算表；公開 GET 僅回版本健康訊息。
- Token 放在 GAS Script Properties 與瀏覽器 sessionStorage，透過 HTTPS POST 本文傳送，不放在 URL、原始碼或匯出資料。
- 前端限制 GAS 網址、採用 CSP 與純文字 DOM 輸出；名冊輸入阻擋公式起始字元。
- 舊版本寫入遭拒絕；完整名冊寫入前備份，寫入後讀回確認。
- `.gitignore` 排除常見名冊、草稿、備份、截圖、憑證和 HAR 網路紀錄。已追蹤的檔案不受排除規則保護，推送前仍須檢查暫存區。

## 使用界線

- 本機密碼（初始值 1234）只是畫面鎖，不是身分驗證或加密。可使用此瀏覽器設定檔的人可能讀到本機資料。
- API Token 是整份應用資料的共用讀寫權限，沒有教師分權或個別帳號；請使用密碼管理器產生 32 字元以上隨機值。
- 草稿、快取與本機密碼存在 localStorage；Token 存在 sessionStorage。相同網站來源的其他程式可能存取這些資料。日後若發布到共用網域，須確認同來源網站都可信，否則使用獨立來源。
- 匯出的草稿、報表和備份含學生資料，即使沒有 Token 仍不可公開。名冊一律使用座號和代稱；試算表保持限制存取。
- GAS 公開端點沒有專用的流量限制或抗濫用服務，未知訪客仍可能消耗執行配額。此架構適合單一教師小規模使用。
- 網路逾時不代表寫入失敗。先保留草稿並讀取雲端核對；衝突時不要反覆重送。

## 若疑似洩漏

立即更換 GAS 的 API_TOKEN，更新使用中的分頁，核對試算表共用權限及異動。不要只刪除 Git 最新檔案就視為處理完成；歷史版本、公開副本及已下載檔案可能仍存在。涉及私人資料的問題請勿附在公開 Issue。

## 2026-09-15 推送前檢查

- Gitleaks 8.30.1：現有 4 筆 Git 提交及工作檔案均未發現憑證命中；工具下載檔與官方 SHA-256 清單一致。
- npm audit：0 個已知漏洞（檢查當時的套件鎖定版本與公告資料）。
- 檔案盤點未發現實際名冊、草稿匯出、HAR、實際 GAS 部署 ID 或試算表網址；測試使用明確標示的假 Token 與代稱。歷史文件含姓名樣式的示例，未發現實際名冊檔案。
- 本次加測所有資料路由拒絕無效憑證，以及健康回應與資料匯出不包含憑證。
- 此檢查不等於滲透測試，也未重新登入檢查使用者實際 GAS 屬性、試算表權限或雲端歷史備份。
